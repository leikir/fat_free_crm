#header
  #welcome
    %span 
      #{t :welcome},
      = current_user.name
    %span> !&nbsp;&nbsp;

    = link_to(t(:quick_find), "#", :id => "jumper") << " |"
    = link_to(t(:blabla), profile_path) << " |"
    = link_to(t(:my_profile), root_path) << " |"
    = link_to(t(:logout), logout_path, :method => :delete)
    = render "/layouts/jumpbox"
  %h3
    = link_to(t(:crm_admin_page), admin_path)
